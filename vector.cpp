#include <bits/stdc++.h>
using namespace std;

int main()
{
    vector<int> v;

    v.push_back(10);
    v.push_back(50);
    v.push_back(30);
    v.push_back(100);


    int v_size=v.size();

    for (int i=0;i<v.size();i++){
        count<<v[i]<<" ";
    }
    cout<<endl<<endl;


    for(auto x:v)
{
    count<<v[i]<<" ";

}


    v.pop_back();

    v.insert(v.begin()+2,90);

    v.erase(v.begin()+4);



return 0;
}

